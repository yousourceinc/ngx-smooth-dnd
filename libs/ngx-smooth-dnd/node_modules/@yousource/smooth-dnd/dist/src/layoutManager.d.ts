import { ElementX, Orientation, LayoutManager } from './interfaces';
import { ContainerOptions } from './exportTypes';
export interface PropMap {
    [key: string]: any;
}
export default function layoutManager(containerElement: ElementX, orientation: Orientation, _animationDuration: number, options: ContainerOptions): LayoutManager;
